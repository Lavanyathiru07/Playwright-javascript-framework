{
  "name": "playwright_ecommerce_website_automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js -p default",
    "test-Reports":"cucumber-js -p default && node utils/report.js",
    "test:retry": "cucumber-js -p default --retry 2",
    "test:fast": "cucumber-js -p default --parallel 3",
    "test:combine":"cucumber-js -p default --parallel 3 --retry 2"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@cucumber/cucumber": "^13.0.0",
    "@playwright/test": "^1.61.0",
    "@types/node": "^26.0.0"
  },
  "dependencies": {
    "chai": "^6.2.2",
    "csv-parser": "^3.2.1",
    "cucumber-html-reporter": "^7.2.0"
  }
}